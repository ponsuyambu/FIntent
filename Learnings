Every fragment transaction has two important properties
1. Name of the fragment to be committed - Each fragment in the fragment manager can have the unique name(replace method).
2. Name of the fragment transaction - A->B - Each fragment transaction(startFragment) should have the unique name(addToBackStack)